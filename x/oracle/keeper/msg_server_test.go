package keeper

import (
	"context"
	"testing"

	"github.com/konstellation/konstellation/x/oracle/types"
)

func setupMsgServer(t testing.TB) (types.MsgServer, context.Context) {
	//keeper, ctx := setupxKeeper(t)
	//return NewMsgServerImpl(*keeper), sdk.WrapSDKContext(ctx)

	return nil, nil
}
